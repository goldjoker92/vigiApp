{
  "name": "vigiapp-functions-default",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "index.js",
  "engines": { "node": "20" },
  "_moduleAliases": { "@": "./src" },
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "logs": "firebase functions:log",
    "predeploy": "npm prune --production && npm dedupe || true",
    "deploy": "npm run predeploy && firebase deploy --only functions:api"
  },
  "dependencies": {
    "express": "^4.19.2",
    "firebase-admin": "^13.5.0",
    "firebase-functions": "^6.5.0",
    "multer": "1.4.5-lts.2",
    "module-alias": "^2.2.2",
    "dotenv": "^17.2.1"
  }
}
