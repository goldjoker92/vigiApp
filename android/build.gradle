// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.google.gms:google-services:4.4.1'
        // Gradle plugin: normally managed by the gradle wrapper. Pin here only if necessary.
        classpath 'com.android.tools.build:gradle'      
        classpath 'com.facebook.react:react-native-gradle-plugin'
        classpath 'org.jetbrains.kotlin:kotlin-gradle-plugin'
    }
}

def reactNativeAndroidDir = new File(
    providers.exec {
        workingDir(rootDir)
        commandLine("node", "--print", "require.resolve('react-native/package.json')")
    }.standardOutput.asText.get().trim(),
    "../android"
)

allprojects {
    repositories {
        maven {
            // React Native android artifacts from node_modules
            url(reactNativeAndroidDir)
        }

        google()
        mavenCentral()
        maven { url 'https://www.jitpack.io' }
    }
}

/*
 * NOTE:
 * - Nous n'appliquons PAS de force sur androidx.browser:browser:1.9.1 (cette version n'est pas disponible pour compileSdk=35).
 * - Pour rester compatible avec ton environnement actuel (compileSdk 35 / AGP 8.8.2), on aligne sur une version stable transitive (1.6.0).
 * - Si tu veux monter à androidx.browser:1.9.x il faudra alors mettre à jour AGP -> 8.9.1+ et compileSdk -> 36 (voir TODO ci-dessous).
 */

/* 
 * Si tu souhaites forcer explicitement une version compatible, décommente et ajuste la ligne ci-dessous.
 * Par défaut je laisse ce bloc commenté pour éviter d'imposer une stratégie globale inutile.
 */
/*
configurations.all {
    resolutionStrategy {
        // Exemple safe: forcer sur la version transitive connue pour ton projet actuel
        // force 'androidx.browser:browser:1.6.0'
    }
}
*/

apply plugin: "expo-root-project"
apply plugin: "com.facebook.react.rootproject"
