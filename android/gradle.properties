# Project-wide Gradle settings.

# JVM / Daemon : plus de mémoire pour builds stables et rapides
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=768m -Dkotlin.daemon.workerIdleTimeout=60000

# Accélération Gradle
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.vfs.watch=true

# AndroidX / Jetifier (nécessaire pour compatibilité RN/Expo)
android.useAndroidX=true
android.enableJetifier=true

# PNG crunch en release
android.enablePngCrunchInReleaseBuilds=true

# Architectures supportées
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# 🚫 On désactive la New Architecture (TurboModules / Fabric)
newArchEnabled=false

# 🚫 On désactive Hermes → JSC sera utilisé
hermesEnabled=false

# Expo image options
expo.gif.enabled=true
expo.webp.enabled=true
expo.webp.animated=false

# Dev client network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Packaging
expo.useLegacyPackaging=false

# Edge-to-edge (pas bloquant si false)
expo.edgeToEdgeEnabled=false

# SDK aligné avec app/build.gradle
android.minSdkVersion=24
android.compileSdkVersion=36
android.targetSdkVersion=36

# Kotlin aligné avec AGP 8.9.1
android.kotlinVersion=1.9.24

# Génération des R classes non transitives → builds plus rapides
android.nonTransitiveRClass=true
